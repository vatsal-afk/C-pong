#include "../include/paddle.h"

void paddle_init(Paddle *paddle, int player) {
    // Initialize the paddle's properties (position, speed, etc.)
    // Depending on the player (1 or 2), set different controls
}

void paddle_update(Paddle *paddle, int direction) {
    // Update the paddle's position based on user input (up/down movement)
}
